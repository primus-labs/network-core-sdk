/** Builder Code from base.dev → Settings → Builder Codes */
export const BASE_BUILDER_CODE = 'bc_dw6vg8s4';

/** Builder codes applied on Base mainnet transactions (ERC-8021). */
export const BASE_BUILDER_CODES: readonly string[] = [BASE_BUILDER_CODE];
